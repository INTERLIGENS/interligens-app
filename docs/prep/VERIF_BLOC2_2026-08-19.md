# Vérification du BLOC 2 — rejouée en lecture seule · 2026-08-19

## PEUT-ON DÉPLOYER — **OUI**

---

**Cible confirmée avant toute lecture.** `system_identifier = 7618684629269072395`,
`neondb`, **178 tables**, `pg_is_in_recovery = false`. Le compte de tables est
lui-même un corroborant : la production en portait **177** avant ce lot, et le
BLOC 1a en ajoute exactement une — `LaundryTrailPublicationLog`.

**Session forcée en lecture seule côté serveur** —
`SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY`, relu à `on`. Aucune
écriture n'était possible, pas seulement interdite. Aucune commande `prisma`
autre que `validate`. `psql` est absent de la machine ; les lectures sont passées
par `pg` (déjà présent dans `node_modules`), sur l'URL poolée, script supprimé
après usage.

---

# 2.7 — LE VERROU A9 · **VERIFIED**

Je le donne en premier parce que c'était le seul ARRÊT possible.

```console
$ npx prisma validate --schema prisma/schema.prod.prisma
CODE DE SORTIE = 1

Error: Prisma schema validation - (get-config wasm)
Error code: P1012
error: Environment variable not found: PRISMA_MIGRATE_INTENTIONNELLEMENT_DESACTIVE_VOIR_CLAUDE_MD.
  -->  prisma/schema.prod.prisma:32
Validation Error Count: 1
[Context: getConfig]
```

Code **1**, erreur **P1012**, à l'étape `getConfig` — avant toute résolution de
`url`, donc avant tout accès réseau. **Ce fichier SQL reste la seule voie de
migration.** Aucune seconde voie vers `ep-bold-sky` ne s'est rouverte.

---

# ÉCART 1 — LE COMPTE D'INDEX · **VERIFIED : ton hypothèse est exacte**

## L'annotation était fausse. La base ne porte aucun index qu'un bloc n'ait créé.

Les 9 index rendus par 2.4, confrontés un à un aux `CREATE INDEX` réellement
présents dans les BLOCS 1a et 1b du fichier :

| Index | Origine | `contype` |
|---|---|---|
| `LaundryTrailPublicationLog_trailId_createdAt_idx` | 1a | — |
| `LaundryTrailPublicationLog_kolHandle_createdAt_idx` | 1a | — |
| `LaundryTrailPublicationLog_reasonCode_idx` | 1a | — |
| `LaundryTrailPublicationLog_contestationRef_idx` | 1a | — |
| `LaundryTrail_publication_idx` | 1a | — |
| `LaundryTrail_kolHandle_publication_idx` | 1a | — |
| `KolProfile_monetaryClaimsPublication_idx` | 1b | — |
| `KolProfile_publication_pair_idx` | 1b | — |
| **`LaundryTrailPublicationLog_pkey`** | **aucun bloc — Postgres** | **`p`** |

**6 (1a) + 2 (1b) + 1 (implicite) = 9.** Exactement ta décomposition.

L'extraction mécanique des `CREATE INDEX` du fichier rend **huit** noms, et ces
huit-là sont, nom pour nom, les huit premiers de la liste. Le neuvième se
distingue par deux propriétés qu'aucun autre n'a : il est le **seul** à porter
une contrainte associée (`pg_constraint.contype = 'p'`) et le seul déclaré
`CREATE UNIQUE INDEX`. Il vient du `"id" TEXT PRIMARY KEY` du `CREATE TABLE`
(ligne 15 du bloc), pas d'un `CREATE INDEX`.

**Contrôle complémentaire, sans filtre de nom** — la table neuve porte
exactement 5 index (4 explicites + le pkey). Aucun index ne se cache derrière un
nom qui échapperait au `LIKE` de 2.4.

**Ce n'est donc pas un ARRÊT.** C'est une annotation que j'avais écrite en
comptant les `CREATE INDEX` sans compter ce que Postgres ajoute de lui-même.
Corrigée dans le fichier (commentaire seul).

---

# ÉCART 2 — LA CONTRAINTE DE PORTÉES · **VERIFIED**

Définition réelle en base, après le seul `DROP`/`ADD` du lot :

```sql
CHECK ((scope = ANY (ARRAY[
  'profile_total'::text, 'summary'::text, 'event'::text, 'involvement'::text,
  'scammed_total'::text, 'case_paid'::text, 'evidence_amount'::text,
  'monetary_all'::text])))
```

| # | Portée attendue | Présente |
|---|---|---|
| 1 | `profile_total` | ✅ |
| 2 | `summary` | ✅ |
| 3 | `event` | ✅ |
| 4 | `involvement` | ✅ |
| 5 | `scammed_total` | ✅ |
| 6 | `case_paid` | ✅ |
| 7 | `evidence_amount` | ✅ |
| 8 | `monetary_all` | ✅ |

**8 sur 8.** Les quatre d'origine sont revenues, les quatre nouvelles sont
posées. Rien n'a été perdu au passage du seul endroit où quelque chose a été
retiré avant d'être remis.

---

# LE BLOC 2 REJOUÉ — 6 requêtes

## 2.1 · **VERIFIED**

| table | colonne | type | `is_nullable` | défaut |
|---|---|---|---|---|
| `KolProfile` | `monetaryClaimsPublication` | `text` | **NO** | `'published'::text` |
| `LaundryTrail` | `publication` | `text` | **NO** | `'published'::text` |

2 lignes, `NO`, `'published'::text`. Conforme.

## 2.2 · **VERIFIED**

3 lignes. Les deux CHECK binaires sont `('published','withdrawn')` ; la
troisième porte les 8 portées (voir écart 2).

## 2.3 · **VERIFIED**

`nb_pk = 1`, `nb_check = 8` (attendu ≥ 7). Les huit, nommés :
`actorId_not_admin`, `actorId_not_blank`, `reasonCode_allowed`,
`reason_not_blank`, `scope_allowed`, `sha_is_sha256`, `status_allowed`,
`transition_is_real`.

Le garde qui refusera `'admin'` comme `actorId` au BLOC 4 est bien en place.

## 2.4 · **VERIFIED après correction de l'annotation** — 9 index, voir écart 1.

## 2.5 · **VERIFIED**

| table | valeur | lignes |
|---|---|---|
| `KolProfile` | `published` | 411 |
| `LaundryTrail` | `published` | **5** |

`'published'` uniquement des deux côtés, **aucune ligne `withdrawn`** : rien n'a
encore été décidé, et le `DEFAULT` n'a produit aucune valeur hors liste.
`LaundryTrail = 5`, exactement ce que la migration A12 annonçait.

## 2.6 · **VERIFIED sur le fond, UNKNOWN sur la confrontation**

`profile_total → 6`. Somme = **6**, une seule portée.

**Corroboré indépendamment :** `KolProfile` porte **6** lignes
`proceedsPublication = 'withdrawn'` (et 405 `published`) — les six décisions du
16 août, une par profil. Les deux comptes concordent.

**UNKNOWN, et je ne le masque pas :** je n'ai pas vu la sortie de ton BLOC 0. Je
ne peux donc pas confronter `6` au `decisions_proceeds` que tu as relevé — je ne
peux qu'établir que **rien entre le BLOC 0 et maintenant n'écrit dans cette
table** (1a et 1b ne font qu'un `DROP`/`ADD` de contrainte), donc la valeur
d'alors ne peut qu'être 6. **Si ton onglet BLOC 0 affichait autre chose que 6,
c'est un ARRÊT** — et toi seul peux le lire.

---

# ÉTAT AVANT LES BLOCS SUIVANTS — lu, pas supposé

| Contrôle | Valeur | Attendu |
|---|---|---|
| `EvidenceItem WHERE r2Key LIKE 'reports/%'` | **0** | 0 — le BLOC 3 n'a pas tourné |
| `KolProceedsPublicationLog WHERE scope='monetary_all'` | **0** | 0 — le BLOC 5 n'a pas tourné |
| `LaundryTrail WHERE publication='withdrawn'` | **0** | 0 — le BLOC 4 n'a pas tourné |

Les garde-fous des BLOCS 3 et 5 passeront. Aucun bloc n'a été exécuté deux fois.

---

# SMOKE §0 — le contrôle d'avant-déploiement · **VERIFIED**

```console
monetaryClaimsPublication  : 62    (attendu > 0)
LaundryTrailPublicationLog : 431   (attendu > 0)
publication dans le scalar enum : 1   (attendu 1)
témoin proceedsPublication : 62
```

Le client généré connaît les trois objets, et le témoin est au même ordre de
grandeur. Arbre sur `main`, **0 fichier suivi modifié**.

---

# CE QUI RESTE UNKNOWN

1. **La confrontation 2.6 au BLOC 0** — voir plus haut. Toi seul as l'onglet.
2. **Le comportement des routes après déploiement.** Tout ce qui précède
   mesure le *schéma* et le *client*, pas une réponse HTTP servie. Les tests 2.1
   à 2.7 de la passe 1 restent à faire, et ils sont la seule chose qui prouve
   que le mode B silencieux — les montants qui passent à `null` sans erreur —
   ne s'est pas produit.
3. **Le journal de build Vercel.** Je n'ai pas vu `vercel-build` s'exécuter. La
   régénération du client en production reste déduite du `package.json`, pas
   observée. Le smoke §0 lit le client **local**.
4. **Les objets R2 eux-mêmes.** Je n'ai vérifié ni l'existence ni les empreintes
   des 32 clés du BLOC 3 : elles viennent d'un inventaire signé du 2026-08-18,
   non rejoué aujourd'hui.

---

# CE QUI A ÉTÉ CORRIGÉ DANS `EXECUTION_2026-08-19.sql`

**Commentaires uniquement — aucune instruction SQL touchée.** Vérifié
mécaniquement : `0` ligne non-commentaire modifiée dans le diff.

- **2.4** — `ATTENDU : 6` → `ATTENDU : 9`, avec la décomposition nom par nom et
  le critère qui distingue le pkey (`contype = 'p'`, `UNIQUE`).
- **BLOC 3** — le titre disait encore *« VERSEMENT DES 34 ARCHIVES »* alors que
  son `STATUS` dit 32. Titre, ligne de sommaire et récapitulatif final alignés
  sur **32 / 34**.
- **STATUS des BLOCS 0, 1a, 1b, 2** — passés de `NON EXÉCUTÉ` à **`EXÉCUTÉ le
  2026-08-19`**, avec la cible nommée (`ep-square-band`, projet
  `plain-hill-77595267`, branche `br-square-dawn`). Un fichier d'exécution qui
  continue d'annoncer « non exécuté » après coup devient un piège pour le
  prochain qui l'ouvre.
